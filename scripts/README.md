# scripts/

Herramientas de una sola vez, fuera del paquete: `seed_cards.py` (fixture de
desarrollo), `reorganize.py` (la reorganización de mazos de agosto de 2026,
guardada como registro), `spike_*.py` (las pruebas de conexión de la fase 0).
Se ejecutan con `uv run python scripts/<nombre>.py`.
